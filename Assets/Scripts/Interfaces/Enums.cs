public enum WindowType
{
    Internet, File, Folder, Feature, Setting, Error, Alert, Vaccine, Trashcan
}
public enum WindowState
{
    Open, Close, Minimize, Maximize
}
public enum IconVisibleType
{
    Normal, Hidden
}